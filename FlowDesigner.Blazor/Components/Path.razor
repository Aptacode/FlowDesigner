@inherits PathBase
<g id="@(Path.Id.ToString())">
    @if (Path.IsShown)
    {
        <polyline points="@Path.Path" style="fill:none;stroke:@(Path.BorderColor.ToKnownColor());stroke-width:@(Path.BorderThickness * Constants.Scale)" />
    }
</g>